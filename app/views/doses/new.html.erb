<h1>Doses#new</h1>

<%= simple_form_for(@dose) do |f| %>
  <%= f.error_notification %>
  <%= f.input :description %>
  <%= f.association :cocktail %>
  <%= f.association :ingredient %>
  <%= f.button :submit %>

<% end %>
